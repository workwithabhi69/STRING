#include <iostream>
#include <vector>
#include <string>
using namespace std;

bool searchFrom(int i, int j, vector<vector<char>>& mat, string word, int dx, int dy) {
    int n = mat.size(), m = mat[0].size();
    for (int k = 0; k < word.size(); k++) {
        int x = i + k * dx, y = j + k * dy;
        if (x < 0 || x >= n || y < 0 || y >= m || mat[x][y] != word[k])
            return false;
    }
    return true;
}

bool searchWord(vector<vector<char>>& mat, string word) {
    int n = mat.size(), m = mat[0].size();
    int dirs[4][2] = {{0,1},{0,-1},{1,0},{-1,0}};
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            for (auto dir : dirs)
                if (searchFrom(i, j, mat, word, dir[0], dir[1]))
                    return true;
    return false;
}

int main() {
    int n, m;
    cin >> n >> m;
    vector<vector<char>> mat(n, vector<char>(m));
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> mat[i][j];
    string word;
    cin >> word;
    if (searchWord(mat, word))
        cout << "Found";
    else
        cout << "Not Found";
    return 0;
}
